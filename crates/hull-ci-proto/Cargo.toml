[package]
name = "hull-ci-proto"
description = "The Hull CI contract v1 types (dispatch + verdict) and the internal control↔node protocol, in one crate so no component drifts from another."
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true

[dev-dependencies]
serde_json.workspace = true
